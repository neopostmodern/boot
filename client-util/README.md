# BOOT: SBC Client Utilities

## Interactive sessions

A utility script is provided to launch an interactive session to test hardware functions. From the project *root* run:

```shell
python3 -m -i 'client-util.init'
```
